apiVersion: keycloak.org/v1alpha1
kind: KeycloakRealm
metadata:
  labels:
    app: sso
  name: sigstore
  namespace: keycloak-system
spec:
  instanceSelector:
    matchLabels:
      app: sso
  realm:
    displayName: Sigstore
    enabled: true
    id: sigstore
    realm: sigstore
